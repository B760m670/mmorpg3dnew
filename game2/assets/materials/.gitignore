# Отсканированные материалы (CC0, ambientCG) в репозиторий НЕ кладутся:
# это ~900 МБ, и git на них захлёбывается. Они полностью воспроизводимы:
#     python3 tools/fetch_materials.py --go
# В репозитории остаётся manifest.json — список того, что должно быть скачано,
# со способом создания и настоящим физическим размером каждого куска.
*/
!created/
!real/
