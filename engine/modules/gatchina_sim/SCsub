# ГАТЧИНА · СИМУЛЯЦИЯ. Наш код на C++ внутри форка движка.
#
# ПОЧЕМУ МОДУЛЕМ ДВИЖКА, А НЕ GDExtension. Причина не в способе доставки: как
# игра попадает на телефон — неподписанным IPA, через LiveContainer или прямо из
# Xcode — на её устройство не влияет никак.
#
# Причины настоящие:
#   1. Мы И ТАК собираем свои шаблоны движка (engine/build_ios_templates.sh) —
#      ради Metal. Модуль не добавляет к сборке ничего. GDExtension добавил бы
#      ВТОРУЮ систему сборки: свой godot-cpp, свои цели под каждую платформу
#      (arm64-устройство, симулятор, macOS, Linux для стенда) и заголовки,
#      привязанные к версии движка.
#   2. Модуль видит ВНУТРЕННОСТИ движка, GDExtension — только открытый API.
#      Разрушаемость и явления упрутся в рендер, а его придётся трогать изнутри.
#      Форк у нас уже есть, значит это право уже оплачено.
#   3. Один продукт сборки вместо двух, которые надо держать в согласии.
# Главное преимущество GDExtension — не пересобирать движок — нам ничего не даёт:
# мы его пересобираем всё равно.
Import("env")

env_gatchina = env.Clone()
env_gatchina.add_source_files(env.modules_sources, "*.cpp")
